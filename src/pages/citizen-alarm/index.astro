---
import { getCollection } from 'astro:content';
import WatchPage from '../../layouts/WatchPage.astro';

const watches = await getCollection('watches');
const entry = watches.find((item) => item.data.slug === 'citizen-alarm');
if (!entry) throw new Error('Citizen Alarm content was not found');
---
<WatchPage watch={entry.data} />
