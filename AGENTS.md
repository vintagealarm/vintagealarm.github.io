# VINTAGE ALARM — 作業エージェント案内

このリポジトリで編集・実装する前に、必ず次を確認する。

1. `SITE_RULES.md`
2. デザイン・表示・操作・motionを触る場合は `DESIGN_ENGINEERING.md`
3. `strategy/seo-aio.md`
4. `measurement/metrics.md`
5. 今回の対象ページと関連資料

## 基本動作

- 既存のOWNER'S NOTE原文、確定済みのキャッチ、資料に基づく本文を、SEO目的だけで書き換えない。
- 変更前に、対象URL・変更ファイル・変更しない事項・合格条件を明確にする。
- 一つの施策を理由にサイト全体を再設計しない。
- 他者の未完了変更を上書きしない。
- 未確認情報、検索需要、順位、流入、成果を推測で埋めない。
- 実装完了・公開完了・成果観測完了を別状態として扱う。
- 公開前にビルド、主要ページ生成、モバイル表示、リンク、メタデータを確認する。
- デザイン変更では、`DESIGN_ENGINEERING.md` の適用範囲、3方向プロトタイプの要否、390px / 320px実寸監査を確認する。
- 未実行の検査は未実行と記録し、成功扱いしない。
- 順位・流入・AI回答での露出を保証しない。

## 検索改善の原則

- 新規記事の量産より、既存ページの不足・導線・証拠を先に確認する。
- 検索意図が同じ言葉違いだけで別ページを量産しない。
- SEOとAIOのための本文水増しをしない。
- AI検索で引用されることと、サイトへ訪問されることを分けて考える。
- 独自性は「所有実機・操作・音・写真・一次資料・観察」に置く。
- X、検索、直接流入など、流入元ごとの役割を混同しない。

## 変更の状態

作業は次の状態を分けて記録する。

- 実装済み
- 検証済み
- 公開済み
- 成果観測済み

公開できたことを、検索順位や来訪増加の成功と表現しない。

## Council / roast 呼び出し

ユーザーが「焼いて」「2ch民で焼いて」「5ch民で焼いて」「スレ民で焼いて」などと依頼した場合は、単なる口調指定ではなく、Council / roast 実行要求として扱う。

- 実行前に `council-worker/README.md` と `council-worker/src/index.ts` の最新仕様を確認する。
- `run_council` が利用可能なら、そのCouncil実行系を使う。
- `run_council` が利用不可・未接続でも、MCP未接続を理由に停止しない。最新Council仕様を読み込み、このチャット内で同じ議論フローを実行する。
- 「再現する」「相当の形式で行う」と断って簡略版へ落とさない。初手の独立判断、返信先の自選、反証、維持 / 修正 / 撤回、複数波の継続議論、最後の議長裁定まで実行する。
- 単なる2ch風口調、一言煽り、賛同だけのレス列、通常の箇条書きをCouncilの代用にしない。
- 時計案件では、参照できるProject資料・PDF・画像・既存実装を一般論より先に確認し、確認済み事実・資料記載・推論・未確認を混ぜない。
- MCPはCouncilを実行するための一つの経路であり、Councilの意味や実行可否そのものを決める前提条件ではない。

## トークン節約

このプロジェクトでは、精度を落とさず無駄なコンテキスト消費を減らす。

- サブエージェント / multi-agent は原則使わない。明示的に許可された場合だけ使う。
- 同じ調査を複数エージェントへ重複依頼しない。
- 既に取得したファイル・検索結果・決定事項を、理由なく再取得しない。
- まず対象ファイルと変更点を絞り、必要な範囲だけ読む。
- 広いリポジトリ全走査は、対象箇所を特定できない場合に限る。
- Web検索は、最新性・一次資料・外部確認が必要な論点に絞る。
- 複数ツール呼び出しは、独立していて必要な場合はまとめ、同じ情報を別手段で重複取得しない。
- 既に十分な証拠が揃ったら探索を止め、実装・検証へ進む。
- 変更後の確認は、影響範囲に応じて最小限から始め、必要な場合だけ全体確認へ広げる。
- 長い説明や作業ログを毎回再掲せず、差分・結論・未確認事項を優先する。
